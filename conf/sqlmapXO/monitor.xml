<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="monitor">

	<insert id="monitor.insertMonitorLog" parameterClass="java.util.Map">
		<selectKey resultClass="java.lang.String" keyProperty="seq">
			SELECT SEQ_MSR_MONITOR_LOG.NEXTVAL FROM DUAL 
		</selectKey>
		INSERT INTO MSR_MONITOR_LOG 
			(MONITOR_SEQ, SERVICE_NAME, LOGFILE_NAME, REG_DATE, ERROR_DATE, LOG_DESC)
		VALUES
			(#seq#, #service_name#, #logFile_name#, sysdate, to_date(#error_date#, 'yyyy/mm/dd hh24:mi:ss'), #log_desc#)	
	</insert>
</sqlMap>